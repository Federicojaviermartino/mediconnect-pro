# MediConnect Pro Environment Variables

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
NODE_ENV=development
PORT=3000

# Session Secret (CHANGE IN PRODUCTION!)
# Generate a strong random secret: openssl rand -base64 32
SESSION_SECRET=mediconnect-demo-secret-key-change-in-production

# =============================================================================
# DATABASE CONFIGURATION - PostgreSQL
# =============================================================================
# Option 1: Use DATABASE_URL (for cloud providers like Render, Heroku)
# DATABASE_URL=postgresql://username:password@hostname:port/database?ssl=true

# Option 2: Use individual PostgreSQL settings (for local development)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=mediconnect
POSTGRES_USER=mediconnect_user
POSTGRES_PASSWORD=dev_password_2024

# PostgreSQL Connection Pool Settings
POSTGRES_POOL_MAX=20
POSTGRES_POOL_MIN=2
POSTGRES_IDLE_TIMEOUT=30000
POSTGRES_CONNECTION_TIMEOUT=10000

# SSL Configuration (set to 'true' for cloud PostgreSQL)
POSTGRES_SSL=false

# =============================================================================
# REDIS CONFIGURATION (for persistent sessions)
# =============================================================================
# Option 1: Use REDIS_URL (for cloud Redis like Render, Heroku, etc.)
# REDIS_URL=redis://username:password@hostname:port

# Option 2: Use individual Redis settings (for local development)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=  # Only if Redis requires authentication

# =============================================================================
# AI SERVICES CONFIGURATION
# =============================================================================
# OpenAI API Key (for Whisper transcription and GPT-4)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (for Claude medical AI)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# =============================================================================
# FEATURE FLAGS
# =============================================================================
# Use PostgreSQL instead of JSON file database (default: false)
USE_POSTGRES=false

# Enable debug logging
DEBUG=false
